$(function () {

    //not available in core
    //initialization
    // text area - classic mode, editor is always visible, will post the value automatically
    // div - inline editor, not able to post the value on form submit, editor will be visible only when clicked on the div
    //$("#txtEditor").kendoEditor();

    //editor tools configuration
    /*$("#txtEditor").kendoEditor({
        tools: [
             "bold",
             "italic",
             "underline",
             "foreColor"
        ]
    });
    */

    
});